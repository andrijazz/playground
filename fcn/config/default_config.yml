config:
    # choose tf or torch
    FRAMEWORK: tf
    # net
    NET: "FCNNet"
    # model
    MODEL: "FCNModel"
    # gpu
    GPU: 0
    # train dataset
    TRAIN_DATASET: "kitti"
    # num classes
    NUM_CLASSES: 29
    # dataset augmentations
    TRAIN_DATASET_AUG: []
    # summary freq
    TRAIN_SUMMARY_FREQ: 100
    # validation freq
    TRAIN_VAL_FREQ: 500
    # save checkpoint model frequency
    TRAIN_SAVE_MODEL_FREQ: -1
    # start saving best val models after x epochs
    TRAIN_START_SAVING_AFTER_EPOCH: 5
    # train batch size
    TRAIN_BATCH_SIZE: 5
    # number of epochs
    TRAIN_NUM_EPOCHS: 1
    # restore from wandb or local
    TRAIN_RESTORE_STORAGE: "local"
    # path to checkpoint from which to be restored
    TRAIN_RESTORE_FILE: ""
    # learning rate
    TRAIN_LR: 0.0001
    # validation batch size
    VAL_BATCH_SIZE: 5
    # test batch size
    TEST_BATCH_SIZE: 5
    # path to checkpoint from which to be restored
    TEST_RESTORE_FILE: ""
    # restore from wandb or local
    TEST_RESTORE_STORAGE: ""
    # dataset for testing
    TEST_DATASET: "kitti"
    # dataset augmentations
    TEST_DATASET_AUG: []
